import React from 'react';
import '../.././../Assets/css/custom.css' // Import the CSS for spinner

const Spinner = () => (
  <div className="spinner-wrapper">
    <div className="spinner"></div>
  </div>
);

export default Spinner;
